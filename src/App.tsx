import { BrowserRouter } from 'react-router';

import RoutesApp from '@/routes/RoutesApp';

const App = () => {
  return (
    <BrowserRouter>
      <RoutesApp />
    </BrowserRouter>
  );
};

export default App;
