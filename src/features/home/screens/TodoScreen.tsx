import { DatePicker, Button } from 'antd';

const TodoScreen = () => {
  return (
    <div>
      <DatePicker placeholder="ok" />
      <Button type="primary">ok</Button>
    </div>
  );
};

export default TodoScreen;
